# Skill entry points

Fewer public skills; one taxon pipeline. Dropped and merged skills are not reintroduced as separate `@` entry points.

**Public entry points:** `@update-pollen` (taxon images/metadata → YAML, pages, sleutels, wiring, validate); `@interactive-pollen-key` (author/edit dichotomous key JSON + page, including OCR/attachment → Beug JSON); `@grill-me` / `@grill-with-docs`; `@domain-modeling`; `@teach-me`; `@handoff`; `@cursor-unignore`.

**Library only** (`disable-model-invocation: true`): `grilling`.

**Discovery:** short when→skill map in `project-context.mdc` plus strong skill `description:` triggers. No separate router skill. Note paths: `rules-notes-boundary.mdc` topic table. Skill authoring: Cursor create-skill plus hygiene bullets in `project-context.mdc`. JSON syntax-only string-preservation: Keys bullet in `project-context.mdc`.

**Rejected:** parallel taxon/page/sleutel skill folders; merging key *authoring* into `@update-pollen`; dedicated `@read-notes`, `@make-skill`, `@fix-json`, or `@beug-key` (OCR rule folded into `@interactive-pollen-key`); dedicated `@palynoquest` skill (contract lives in `docs/naslag/palynoquest-modes.md`; agents read that page via the SoT row in `project-context.mdc`).
